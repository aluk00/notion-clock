{
  "name": "notion-clock",
  "version": "1.0.0",
  "description": "Notion to Firestore sync and dashboard tools",
  "scripts": {
    "sync": "node notion-firestore-sync.js",
    "sync:staff": "node notion-firestore-sync.js --collection=staff_directory",
    "sync:projects": "node notion-firestore-sync.js --collection=projects",
    "sync:dry": "node notion-firestore-sync.js --dry-run"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
